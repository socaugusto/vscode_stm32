{
    "tasks": [
        {
            "type": "shell",
            "label": "Build and flash",
            "command": "make",
            "args": [
                "flash",
                "-j8"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ],
    "version": "2.0.0"
}